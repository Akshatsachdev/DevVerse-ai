name: DevVerse AI CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Backend structure check
        run: echo "Backend CI ready"

      - name: Frontend structure check
        run: echo "Frontend CI ready"
